## These SQL queries are examples of some of the queries that were used to build the semantic model from the Fabric Lakehouse.

These queries were built to hit the Fabric Lakehouse in order to build tables in the Semantic Model
